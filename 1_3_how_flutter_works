Native Framework:
    운영체제와 직접 대화, 버튼 등 모든 요소 생성 요청을 운영체제에 직접적으로 함.
Flutter, Dart:
    게임 엔진처럼 동작
    Dart 코드는 Flutter Framework(애니메이션, 페인팅 등 요소 포함)에서 이용
    실제로 각 요소를 화면에 표시할때는 운영체제에 직접 요청하지 않음. -> 엔진 이용
    Flutter 엔진에서 실제 UI 렌더링을 책임짐.
    (운영체제에 내장된 Widget을 사용하지 않는다.)

    사용자가 앱을 실행시키면 runner 프로젝트를 실행
    -> runner 프로젝트는 엔진을 실행
    -> 엔진에서 UI를 렌더링

    runner 프로젝트 = Embedder
    플랫폼에 특화되어있음.

    네이티브 위젯을 사용하지 않는 문제점. 부자연스러움. (비슷하게 구현하려고 했으나 실패)
    화면 상의 모든 픽셀을 통제 할 수 있는 장점이기도 함. 운영체제에 대한 제약이 없음.